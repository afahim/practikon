document.onkeydown = function(evt) {
    evt = evt || window.event;
    switch (evt.keyCode) {
        case 13: //Enter
        	
        	break;
        case 37: //Left Key

            break;
        case 39: //Right Key

            break;
    }
};
